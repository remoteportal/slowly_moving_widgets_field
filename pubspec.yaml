name: slowly_moving_widgets_field
description: Animated widgets slowly moving about, sometimes colliding and bouncing (like vintage "asteroids game"), for an interesting dynamic user interface for unusual menus, etc.
version: 0.0.1
author: Peter Alvin <github_public@onlyup.com>
homepage: https://github.com/remoteportal/substring_highlight

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: